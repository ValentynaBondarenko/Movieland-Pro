spring:
  profiles:
    active: default
  datasource:
    url: jdbc:postgresql://localhost:8200/movieland?characterEncoding=UTF-8
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  main:
    banner-mode: off
  liquibase:
    change-log: classpath:db/changelog/changelog.xml
  jpa:
    show-sql: false
    #The session is closed early — I need to load everything manually.
    open-in-view: false
    hibernate:
      ddl-auto: none
  data:
    redis:
      host: localhost
      port: 6379

liquibase:
  change-log: classpath:changelog.xml
  outputEncoding: UTF-8
  enabled: true

logging:
  config: classpath:logback.xml
  level:
    liquibase: INFO

movieland:
  movie:
    random:
      limit: 3
    enrichment:
      timeout: 5
  cache:
    update:
      interval: ${CACHE_UPDATE:14400000} # updated every 4 hours
  security:
    token:
      expiry-ms: 7200000 # expired after 2 hours
      secret:  ${MOVIELAND_TOKEN_SECRET:Vu+L6+nRgPsWlCbPMV4vhOiLFZ+P7LOkgNs++tDnmWA=}
exchange:
  api:
    url: https://bank.gov.ua/NBUStatService/v1/statdirectory/exchangenew?json
  scheduled:
    cron: 0 0 0  * * ? #update every day at 00:00
  zone: Europe/Kiev
  cache:
    timeout: 30 #30min
